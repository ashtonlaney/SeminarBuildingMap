@page "{bdId}"
@model SeminarBuildingMap.Areas.Admin.Pages.BuildingEditDisplayModel
@{
}

<form method="post">
    <div class="form-group">
        <label>Building ID:</label>
        <input class="form-control" value="@RouteData.Values["bdId"]" readonly />
    </div>
    <div class="form-group">
        <label>Building Name:</label>
        <input class="form-control" asp-for="bdName" required/>
    </div>
    <button type="submit" class="btn btn-primary" asp-page-handler="update">Update Building Name</button>
</form>

@(Html
    .Grid(Model.FloorData)
    .Build(columns =>
    {
        columns.Add(model => model.flNo).Titled("Floor Number");
        columns.Add(model => model.flName).Titled("Floor Name");
    })
    .Empty("No Floors Found For This Building")
    .Sortable()
    .Pageable()
    .Filterable()
)

<form method="post">
    <div class="form-group">
        <label>Floor Number:</label>
        <input class="form-control" asp-for="flNo" />
    </div>
    <button type="submit" class="btn btn-primary" asp-page-handler="add">Add New Floor</button>
</form>


@section Scripts
{
    <script src="~/js/mvc-grid/mvc-grid.js"></script>
    <script>
        [].forEach.call(document.getElementsByClassName('mvc-grid'), function (element) {
            new MvcGrid(element);
        });
    </script>
    <script>
        document.addEventListener('rowclick', function (e) {
            window.location.href = "../FloorEditDisplay/" + "@RouteData.Values["bdId"]" + "/"  + e.detail.data['fl-no'];
        });
    </script>

}